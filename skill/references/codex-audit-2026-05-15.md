# Codex Audit Findings — PENdp V3/V4 (2026-05-15)

> Source: `CODE_AUDIT_REPORT.md` generated by Codex (gpt-5.5) via `codex exec`

## Overall: 5/10

Core idea coherent, modules compile, but critical state-safety, CLI, and correctness issues.

## Critical Issues (5)

| # | Issue | Location | Impact |
|:--|:------|:---------|:-------|
| 1 | Gate calibration mutates shared `PENDP_GATES` globally | `gates.py:237-239` | Concurrency unsafe, non-reproducible |
| 2 | CLI crashes before scoring due to eager MLX import | `cli.py:120-121` | `pendp score` unusable on non-MLX systems |
| 3 | `--rank` computes but prints nothing | `cli.py:249-265` | Silent failure |
| 4 | `structure_score("")` → KeyError crash | `structure.py:203-213` | Empty input crash |
| 5 | `evolve(rounds=0)` → IndexError | `evolution.py:233-236` | Zero rounds crash |

## Key Warnings (selected)

| # | Issue | Detail |
|:--|:------|:-------|
| W1 | **Residue counting: types not frequency** | `sum(1 for aa in "AVILMFWY" if aa in seq)` counts unique types. `AAAAAAAAAA` = 1 hydrophobic, not 10. Affects D2/D3/D4/D6 + structure.py. |
| W2 | No sequence validation | `"!!!!"` passes scoring with plausible scores |
| W4 | Evolution sort order wrong | Computes `improvement` delta but sorts by absolute `gate_score` |
| W6 | Gate metrics inconsistent after early critical fail | `total_gates` ≠ configured count; `critical_pass_count` conflates critical+important |

## Design Feedback

- GatePipeline should be a pure evaluator, not a global threshold mutator
- Scoring/Structure/Evolution mix scientific logic with emoji display — separate data layer
- Evolution engine needs explicit optimization objective (delta vs absolute)
- CLI should lazy-import heavy subsystems

## Fix Priority

| Priority | Issues | Effort |
|:---------|:-------|:-------|
| P0 | W1 (residue count) + #1 (gate mutation) | ~50 lines |
| P1 | #2 (MLX import) + #3 (--rank output) + #4, #5 (empty/zero input) | ~40 lines |
| P2 | W2 (validation) + W4 (evolution sort) | ~30 lines |
